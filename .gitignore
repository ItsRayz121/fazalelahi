# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Node (only if you add tooling later)
node_modules/
npm-debug.log*

# Local env / secrets (NEVER commit private keys)
.env
.env.local
*.local.env

# Logs / temp
*.log
.cache/

# Original full-resolution event archive (kept locally, NOT pushed).
# Only the web-optimized copies in assets/events/ are deployed.
Fazal Elahi Events/

.vercel
